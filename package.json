{
  "name": "unify",
  "module": "index.ts",
  "type": "module",
  "devDependencies": {
    "bun-types": "^1.0.2"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "elysia": "^0.7.2",
    "yargs-parser": "^21.1.1"
  },
  "scripts": {
    "compile": "bun build ./src/index.ts --compile --outfile unify && sudo mv unify /usr/local/bin"
  }
}